enum Direction {
	up = 'up',
	down = 'down',
	left = 'left',
	right = 'right',
}

console.log(Direction.up)
console.log(Direction.up == 'up')

// 常量枚举

const enum Direction2 {
	up,
	down,
	left,
	right,
}
